<div class="dashboard">


<a href="#" class="btn btn-success glyphicon glyphicon-menu-hamburger" id="menu-toggle"></a>

<div class="sidebar">
  <ul class="sidebar-nav">
    <li><a class="glyphicon glyphicon-plus" href='/books/new'></a>
        <a class="" href='/books/new'>Add a Book</a>
    </li>
    <li><a class="glyphicon glyphicon-user" href="/logout"></a>
        <a class="" href="/logout">Log Out</a>
    </li>
  </ul>
</div>


<!-- <div class="shelf">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <% books.forEach((oneBook) => { %>
        <div class="book">
          <img src="<%= oneBook.img_path %>" alt="<%= oneBook.img_name %>" />
          <p>URL: <%= oneBook.img_path %> </p>
          <%= console.log("this is it" + oneBook.img_path) %>
          <p>Title: <%= oneBook.title %></p>
          <p>Author: <%= oneBook.author %> </p>
          <button><a href="/books/<%= oneBook._id %>">Playlist</a></button>
          <form action="/books/<%= oneBook._id %>/delete" method="POST">
            <button type="submit">Delete Book</button>
          </form>
          <form action="/books/<%= oneBook._id %>/edit" method="GET">
            <button type="submit">Edit Book</button>
          </form>
        </div>
        <% }) %>
      </div>
    </div>
  </div>
</div> -->

<section class="books">

  <% books.forEach((oneBook) => { %>


    <div class="oneBookInfo">

      <div class="oneBookImg">
        <img src="<%= oneBook.img_path %>" alt="<%= oneBook.img_name %>" />
      </div>

      <br>

      <div class="information">
        <!-- <h5>Title: <%= oneBook.title %></h5>
        <p>Author: <%= oneBook.author %> </p> -->
        <button class="glyphbtn"><a class="glyphbtn glyphicon glyphicon-play" href="/books/<%= oneBook._id %>"></a>
          <a href="/books/<%= oneBook._id %>"></a>
        </button>

        <form class="glyphbtn" action="/books/<%= oneBook._id %>/delete" method="POST">
          <button type="submit"><div class="glyphbtn glyphicon glyphicon-trash"</div></button>
        </form>

        <form class="glyphbtn" action="/books/<%= oneBook._id %>/edit" method="GET">
          <button type="submit"><div class="glyphbtn glyphicon glyphicon-edit"></div></button>
        </form>
      </div>

    </div>

  <% }) %>
</section>
